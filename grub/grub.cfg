# Boot the first menu entry immediately.
set timeout=0
set default=0

menuentry "construction-os" {
    # Load our ELF kernel through the Multiboot 1 protocol.
    multiboot /boot/kernel.elf

    # Transfer control to the kernel entry point.
    boot
}
